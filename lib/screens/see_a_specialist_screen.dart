import 'package:flutter/material.dart';

class SeeSpacialistScreen extends StatelessWidget {
  const SeeSpacialistScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
